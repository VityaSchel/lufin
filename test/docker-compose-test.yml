services:
  test:
    build:
      context: ../
      dockerfile: test/Dockerfile
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - lufintest

networks:
  lufintest:
    driver: bridge
