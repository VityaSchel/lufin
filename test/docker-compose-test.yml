services:
  test:
    build:
      context: ../
      dockerfile: test/Dockerfile
    environment:
      - BACKEND_URI=http://backend:4000
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - lufin

networks:
  lufin:
    driver: bridge
