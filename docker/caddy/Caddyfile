{
	auto_https {$AUTO_HTTPS:}
	admin off
}

{$DOMAIN} {
	handle_path /api/* {
		reverse_proxy backend:{$BACKEND_PORT}
	}
	handle {
		root * /srv
		try_files {path} /index.html
		encode
		file_server
	}
}
